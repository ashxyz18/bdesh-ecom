[build]
builder = "nixpacks"

[deploy]
startCommand = "cd packages/database && npx prisma migrate deploy && cd ../../apps/web && npx next start -p $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
