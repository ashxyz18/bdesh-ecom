[deploy]
startCommand = "/bin/sh -c 'cd /app/packages/database && /app/node_modules/.bin/prisma migrate deploy && cd /app/apps/web && node server.js'"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
